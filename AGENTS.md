# InvestiGator

## Cursor Cloud specific instructions

This is a greenfield repository. As of initial setup, the repo contains only an empty `Playbook` file with no application code, dependencies, or configuration.

- **No services to run.** There are no backend, frontend, or database services.
- **No dependencies to install.** There is no `package.json`, `requirements.txt`, `go.mod`, or similar.
- **No tests, lint, or build commands available.**

Future agents should re-evaluate the repository contents when starting a session, as application code may have been added since this file was written.
