export async function logAudit(
  action: string,
  entityType: string,
  entityId: string,
  metadata?: Record<string, unknown>,
) {
  void action;
  void entityType;
  void entityId;
  void metadata;
  return;
}
